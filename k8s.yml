# TODO: define storage via PersistentVolumeClaims (replicate?)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tts-ui
spec:
  selector:
    matchLabels:
      app: tts-ui
  template:
    metadata:
      labels:
        app: tts-ui
    spec:
      containers:
        - name: tts-ui
          image: sthinds/tts-ui:arm64
          imagePullPolicy: Always
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: tts-ui
spec:
  type: NodePort
  selector:
    app: tts-ui
  ports:
    # TODO: is this right?
    - port: 8080
      targetPort: 80
      name: tts-ui
  externalIPs:
    - 192.168.1.240
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: default
  name: tts-ui-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    kubernetes.io/ingress.class: "nginx"
spec:
  defaultBackend:
    service:
      name: tts-ui
      port:
        # TODO: what port?
        number: 8080
  rules:
    - host: tts-ui
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tts-ui
                port:
                  number: 8080
